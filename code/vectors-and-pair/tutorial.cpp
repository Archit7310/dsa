#include <bits/stdc++.h>

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

int main()
{
    fastio;
    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);

    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i];
    // }
    int a = -12 * -12;
    cout << a;
    bool arr[1000000] = {false};
    bool arrn[1000000] = {false};
    cout << arrn[6];

    return 0;
}